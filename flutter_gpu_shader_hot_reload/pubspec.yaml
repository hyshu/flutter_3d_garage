name: flutter_gpu_shader_hot_reload
description: ""
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0 <3.48.0"

dependencies:
  flutter: { sdk: flutter }
  flutter_gpu: { sdk: flutter }
  hooks: 2.0.2
  vector_math: 2.4.2

dev_dependencies:
  flutter_test: { sdk: flutter }
  flutter_lints: 6.0.0
flutter:
  uses-material-design: true
  assets:
    - build/shaderbundles/
